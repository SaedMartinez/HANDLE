SELECT t_transaction.id_transaction,
	   t_transaction.serial_number
       t_transaction.date,
       t_user.name,
       t_user.id_user
FROM t_transaction 
INNER JOIN t_user
ON t_transaction.id_user=t_user.id_user;




SELECT tran_x_prod.quantity_t,
       t_product.id_product,
       t_product.name
FROM tran_x_prod
INNER JOIN t_product
ON tran_x_prod.id_product=t_product.id_product WHERE tran_x_prod.id_transaction=?;